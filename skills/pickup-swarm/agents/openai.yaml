interface:
  display_name: "Pickup Swarm"
  short_description: "Recover workflows and prepare safe continuation"
  default_prompt: "Use $pickup-swarm to check the latest checkpoint and relevant changes, recover only missing or invalid context, and return one bounded receipt to the active execution loop."
